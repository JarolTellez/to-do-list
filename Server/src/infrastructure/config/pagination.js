 const PAGINATION_CONFIG = {
  MAX_LIMIT: 100,        // Limite maximo para cualquier consulta
  DEFAULT_LIMIT: 10,     // Limite por defecto
  DEFAULT_PAGE: 1,       // Pagina por defecto
  MAX_PAGE_SIZE: 200     // Podrías añadir más configuraciones si necesitas
};

module.exports = PAGINATION_CONFIG;