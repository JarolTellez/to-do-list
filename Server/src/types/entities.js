/**
 * Centralized type definitions for domain entities
 * @module types/entities
 */

/**
 * @typedef {import('../domain/entities/user')} User
 * @typedef {import('../domain/entities/tag')} Tag
 * @typedef {import('../domain/entities/userTag')} UserTag
 * @typedef {import ('../domain/entities/session')} Session
 * @typedef {import('mysql2/promise').Connection} Connection
 * @typedef {import('mysql2/promise').Pool} Pool
 * @typedef {import('../config/connectionDB')} ConnectionDB
 */

// Exportar para usar en otros lugares si es necesario
module.exports = {};